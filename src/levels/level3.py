# -*- coding: utf-8 -*-

'''
LEVEL 3: shortest path between two values in a tree

Return the list of keys from start to goal (inclusive).
If missing: return []
If same: [start]

Use BFS to build a parent map, then reconstruct path.
'''

from src.structures.queue import Queue

def shortest_path(B, start, goal):
    # TODO
    return None
